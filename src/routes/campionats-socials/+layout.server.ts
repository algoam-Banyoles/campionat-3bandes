import type { LayoutServerLoad } from './$types';

export const load: LayoutServerLoad = async () => {
  // Simplificar el layout server - la verificació es farà al client
  return {
    // Layout data if needed
  };
};